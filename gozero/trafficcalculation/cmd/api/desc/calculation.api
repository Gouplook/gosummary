syntax = "v1"

import (
	"calculation/calculation.api"
)

@server (
	prefix : calculation/v1
)

service  caclulation{
	@doc "获取路段拥堵状态"
	@handler  GetTrafficStatus
	get /traffic/status  (TrafficStatusReq) returns (TrafficStatusResp)
}